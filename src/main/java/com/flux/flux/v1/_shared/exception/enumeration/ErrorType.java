package com.flux.flux.v1._shared.exception.enumeration;

public enum ErrorType {
    VALIDATION_ERROR,
    INTERNAL_SERVER_ERROR,
    ACCESS_DENIED,
    ALREADY_EXISTS,
    NOT_FOUND,
    UNAUTHORIZED,
}
